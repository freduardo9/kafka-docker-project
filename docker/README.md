# Kafka with Docker

## Steps to Run Kafka and Zookeeper
1. Navigate to the `docker` folder.
2. Run:
   ``` sh
   docker-compose up -d
   ```
3. To stop the services, run:
    ``` sh
    docker-compose down
    ```